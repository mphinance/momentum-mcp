# momentum-mcp: Quantitative Trading MCP Server
# -----------------------------------------------

# MCP server framework
fastmcp>=3.0

# Stock screening
tradingview-screener>=3.0

# Market data
yfinance>=0.2

# Data manipulation
pandas>=2.0

# Technical analysis indicators
# NOTE: pandas-ta hard-pins numba==0.61.2 which doesn't support Python 3.14.
# Install with --no-deps and use numba>=0.62 instead (verified compatible).
pandas-ta>=0.3
numba>=0.62

# Candlestick chart rendering
mplfinance>=0.12
matplotlib>=3.7

# News & article extraction
feedparser>=6.0
trafilatura>=2.0

# Required by mcp_server/options_greeks.py (Black-Scholes)
scipy>=1.10

# Required by mcp_server/tv_analysis.py
tradingview-ta>=3.3

# Required by mcp_server/knowledge.py
chromadb>=1.0

# Required by mcp_server/edgar.py (SEC EDGAR — no API key, just a User-Agent string)
requests>=2.31

# Required by mcp_server/tickertrace.py (api.tickertrace.pro — no key, no account)
httpx>=0.27
